{
  "name": "rtnode",
  "version": "1.0.0",
  "description": "Utility for using the Request Tracker REST API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "= <=> (=)",
  "license": "MIT",
  "dependencies": {
    "read": "^1.0.7",
    "request": "^2.76.0",
    "url-join": "^1.1.0"
  }
}
